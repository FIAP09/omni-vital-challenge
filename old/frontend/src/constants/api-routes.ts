export const API = { auth: { login: '/auth/login', me: '/auth/me', logout: '/auth/logout' }, dashboard: { summary: '/dashboard/summary' }, surgeries: { list: '/surgeries', monitor: (id: number) => `/surgeries/${id}/monitor`, detail: (id: number) => `/surgeries/${id}` }, patients: { list: '/patients', detail: (id: number) => `/patients/${id}` }, alerts: { list: '/alerts', resolve: (id: number) => `/alerts/${id}/resolve` }, vitals: { history: (patientId: number) => `/patients/${patientId}/vitals` } } as const
