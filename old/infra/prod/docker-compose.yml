services:
  omni-vital-frontend:
    image: omni-vital/frontend:latest
    ports:
      - "80:80"
    environment:
      - VITE_API_URL=${VITE_API_URL}
      - VITE_APP_ENV=production
    restart: always
